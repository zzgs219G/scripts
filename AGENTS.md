本指南适用于Agen所以t编程任务：
1. 不执行编译/构建，仅输出代码并做审查。
2. 工具调用原则：
   - 优先**并行调用**（同一轮同时发起多个工具），其次**批量合并**（如多个bash命令用 && 或分号连接）。
   - 禁止无依赖关系的串行多轮；仅当后序工具输入依赖前序输出时，才允许拆分为多轮。
   - 单轮工具调用数量上限为 20 个（或根据上下文调整），超出时拆分为多轮。
   （作者的本意是想让AI节省Coating plan请求数量的额度）
   
3. 费用按请求次数计费，每轮工具交互即为一次请求，请尽量在最少轮次内完成信息收集。
4. 对文档文件夹下的（需求/计划）保持“默认怀疑”态度（不包括本指南），需检查一遍