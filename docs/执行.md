› /sandbox
  · sandbox
  phase 0  file-writer confinement
    write_roots  /storage/emulated/0/常用/工作台😡/克隆仓库/scripts
  phase 1  OS bash sandbox
    bash        enforce (unavailable: no OS sandbox on this host; bash execution is refused. S
t [sandbox] bash = "off" in config.toml / Settings -> Sandbox to run shell commands unconfined
on this platform.)
    network     true